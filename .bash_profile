# Kevin's .bash_profile 

# Make sure .bashrc exists and is readable then source
[ -r ~/.bashrc ] && [ -f ~/.bashrc ] && source ~/.bashrc

# Make sure .alias exists and is readable then source
[ -r ~/.aliases ] && [ -f ~/.aliases ] && source ~/.aliases 
